language: javascript
notifications:
  slack: 
    rooms: 
      - ${process.env.TRAVISCIPD}#github
      - ${process.env.TRAVISCIPD}#travis-ci
addons:
  apt:
    packages:
      - nodejs
      - npm
script:
  - ant test